# Third-Party Notices

This Sprint 12A notice is an engineering inventory, not a final legal opinion. Legal review remains
required before public distribution.

The application includes open-source Python, JavaScript, Rust, Tauri, WebView, PyInstaller, and
Playwright tooling governed by their respective package licences. Exact dependency versions are
recorded in `requirements.lock`, `requirements-sidecar.lock`, `frontend/package-lock.json`, and
`frontend/src-tauri/Cargo.lock`.

The project source is distributed under the repository `LICENSE` (MIT). Bundled offline fixtures
are project-created synthetic records, contain no licensed market data, credentials, or personal
data, and use the project licence. No separate WebGL library is bundled; rendering uses the browser
WebGL API.

Before public release, generate complete dependency licence reports on each target platform and
have counsel review attribution, redistribution, PyInstaller bootloader, WebKit, and native system
framework obligations.
