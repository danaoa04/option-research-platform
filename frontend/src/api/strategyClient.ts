import type{ExpirationSummary,OptionContractRow,StrategyDraft,StrategyPreview,StrategyTemplate,UnderlyingSnapshot}from"../types/strategy";import{optionChain,strategyExpirations,strategyTemplates,strategyUnderlyings}from"../fixtures/strategyDemo";
export interface StrategyApiClient{underlyings(signal?:AbortSignal):Promise<UnderlyingSnapshot[]>;expirations(symbol:string,signal?:AbortSignal):Promise<ExpirationSummary[]>;chain(symbol:string,expiration:string,signal?:AbortSignal):Promise<OptionContractRow[]>;catalogue(signal?:AbortSignal):Promise<StrategyTemplate[]>;validate(draft:StrategyDraft,signal?:AbortSignal):Promise<{status:"valid"|"valid_with_warnings"|"incomplete"|"invalid"|"unsupported";warnings:string[]}>;preview(draft:StrategyDraft,signal?:AbortSignal):Promise<StrategyPreview>;save(draft:StrategyDraft,signal?:AbortSignal):Promise<{workspaceId:string;checksum:string}>}
export function createFixtureStrategyClient():StrategyApiClient{return{underlyings:async()=>structuredClone(strategyUnderlyings),expirations:async()=>structuredClone(strategyExpirations),chain:async(_symbol,expiration)=>structuredClone(optionChain.filter(x=>x.expiration===expiration)),catalogue:async()=>structuredClone(strategyTemplates),validate:async draft=>({status:draft.legs.length?"valid_with_warnings":"incomplete",warnings:["Synthetic fixture validation — backend validation required"]}),preview:async()=>({netMidpoint:null,cashFlow:"unknown",maxProfit:null,maxLoss:null,breakevens:[],greeks:{delta:0,gamma:0,theta:0,vega:0,rho:0},warnings:["Backend preview required"],synthetic:true}),save:async draft=>({workspaceId:`fixture-${draft.id}`,checksum:"fixture-checksum-not-reproducible"})}}
