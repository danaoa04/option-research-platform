import{fireEvent,render,screen}from"@testing-library/react";import{MemoryRouter}from"react-router-dom";import{describe,expect,it,vi}from"vitest";import{checkCompatibility}from"../api/compatibility";import{HttpClient,StructuredApiError}from"../api/httpClient";import{App}from"../app/App";import{loadRuntimeConfig}from"../config/runtime";import{pollJob}from"../hooks/jobPolling";import{createDiagnosticBundle,redact}from"../logging/diagnostics";import{DebouncedWorkspaceSaver,detectWorkspaceConflict,validateWorkspaceImport,type WorkspaceDocument}from"../state/workspaces";
const config=loadRuntimeConfig({VITE_BACKEND_URL:"http://127.0.0.1:8000",VITE_API_VERSION:"v1",VITE_FIXTURE_MODE:"true"});
describe("production integration boundaries",()=>{it("validates explicit runtime modes without secrets",()=>{expect(config.fixtureMode).toBe(true);expect(config.backendBaseUrl).toBe("http://127.0.0.1:8000");expect(()=>loadRuntimeConfig({VITE_BACKEND_URL:"not-a-url"})).toThrow()});it("adds version and request headers and structures backend failures",async()=>{const transport=vi.fn(async(_input:RequestInfo|URL,init?:RequestInit)=>{expect(new Headers(init?.headers).get("X-API-Version")).toBe("v1");expect(new Headers(init?.headers).get("X-Request-ID")).toBeTruthy();return new Response(JSON.stringify({ok:true}),{status:200,headers:{"Content-Type":"application/json"}})});const client=new HttpClient(config,transport as typeof fetch);await expect(client.request("/health","GET",undefined,{safeRead:true})).resolves.toEqual({ok:true});const failing=new HttpClient(config,vi.fn(async()=>new Response("",{status:409}))as typeof fetch);await expect(failing.request("/workspace","PUT",{},{})).rejects.toBeInstanceOf(StructuredApiError)});it("falls back only when fixture mode is explicit",async()=>{const failing=new HttpClient(config,vi.fn(async()=>{throw new TypeError("offline")})as typeof fetch);expect((await checkCompatibility(failing,config)).state).toBe("fixture_only_fallback");const production={...config,fixtureMode:false};expect((await checkCompatibility(new HttpClient(production,vi.fn(async()=>{throw new Error("offline")})as typeof fetch),production)).state).toBe("backend_unavailable")});it("detects workspace conflicts and validates imports",()=>{const local:WorkspaceDocument={workspaceId:"w1",workspaceType:"volatility",schemaVersion:1,version:2,checksum:"a",updatedAt:"2026-01-01",payload:{tab:"surface"}};expect(detectWorkspaceConflict(local,{...local,version:3})).toBe("server_newer");expect(detectWorkspaceConflict(local,{...local,checksum:"b"})).toBe("checksum_mismatch");expect(validateWorkspaceImport(local)).toEqual(local);expect(()=>validateWorkspaceImport({...local,schemaVersion:2})).toThrow(/schema/)});it("autosaves only valid compatible workspaces",async()=>{vi.useFakeTimers();const save=vi.fn(async()=>undefined);const saver=new DebouncedWorkspaceSaver(500,save);const document={workspaceId:"w1",workspaceType:"research",schemaVersion:1,version:1,checksum:"c",updatedAt:"now",payload:{}}as WorkspaceDocument;saver.schedule(document,false,true);await vi.advanceTimersByTimeAsync(600);expect(save).not.toHaveBeenCalled();saver.schedule(document,true,true);await vi.advanceTimersByTimeAsync(600);expect(save).toHaveBeenCalledOnce();vi.useRealTimers()});it("polls adaptively and stops at terminal state",async()=>{const states:Array<"running"|"completed">=["running","running","completed"];const fetchStatus=vi.fn(async()=>({value:"job",state:states.shift()??"completed"}));const result=await pollJob(fetchStatus,{signal:new AbortController().signal,baseMs:1,maxMs:3,maximumRetries:2,wait:async()=>undefined});expect(result.state).toBe("completed");expect(result.attempts).toBe(3)});it("redacts diagnostics",()=>{expect(redact("api_key=abc /Users/dana/private")).toBe("api_key=[REDACTED] /Users/[REDACTED]/private");const bundle=createDiagnosticBundle(config,"offline_demo",{state:"fixture_only_fallback",backendVersion:null,apiVersion:null,schemaVersion:null,migrationStatus:"unknown",availableEndpoints:[],missingOptionalEndpoints:[],mutationsAllowed:false,checkedAt:"now"});expect(JSON.stringify(bundle)).not.toContain("credential=")});it("opens the accessible command launcher via keyboard",()=>{render(<MemoryRouter><App/></MemoryRouter>);fireEvent.keyDown(window,{key:"k",metaKey:true});expect(screen.getByRole("dialog",{name:"Command launcher"})).toBeInTheDocument();fireEvent.change(screen.getByRole("textbox",{name:"Find command"}),{target:{value:"diagnostics"}});expect(screen.getByRole("button",{name:"Open diagnostics"})).toBeInTheDocument();fireEvent.keyDown(window,{key:"Escape"});expect(screen.queryByRole("dialog")).not.toBeInTheDocument()})});
