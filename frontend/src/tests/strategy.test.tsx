import{fireEvent,render,screen}from"@testing-library/react";import{MemoryRouter}from"react-router-dom";import{describe,expect,it}from"vitest";import{StrategyWorkspace}from"../pages/StrategyWorkspace";
describe("strategy workspace",()=>{it("renders a labelled synthetic chain and adds a leg",()=>{render(<MemoryRouter><StrategyWorkspace/></MemoryRouter>);expect(screen.getByRole("table",{name:/Synthetic option chain/})).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Add 2026-08-21 500 call from bid"}));expect(screen.getByText(/1× 2026-08-21 500 call/)).toBeInTheDocument();expect(screen.getAllByText(/synthetic/i).length).toBeGreaterThan(0);});});
